[project]
name = "isaac-sim"
version = "0.1.0"
description = ""
authors = [
    {name = "Scott Adams",email = "scott.adams@neuralfoundry.co.uk"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]

[tool.poetry]
packages = [{include = "isaac_sim", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
